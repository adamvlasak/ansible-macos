set -g default-shell {{ shell.command }}
set-option -sa terminal-overrides ",{{ shell.term }}:Tc"

set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded!"
